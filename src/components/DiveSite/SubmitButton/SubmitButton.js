import React from 'react';

  const SubmitButton = ( props ) => {
      return (
          <div>
            <p>SubmitButton.js</p>
            { props.children }
          </div>
      );
  }

  export default SubmitButton;